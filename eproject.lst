;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("eproject" "/Users/tony/.emacs.d/elpa/eproject-0.4")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 0)
        (left . 0)
        (width . 179)
        (height . 50)))

